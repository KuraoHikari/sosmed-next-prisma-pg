'use Client';

import { Modal } from '../';

const RegisterModal = () => {
  return (
    <Modal
      isOpen={true}
      //   onClose={() => {}}
      //   onSubmit={() => {}}
      actionLabel="Register"
    />
  );
};
export default RegisterModal;
